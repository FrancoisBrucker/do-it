---
layout: layout/post.njk

tags: 
    - "POK"
    - "index"

eleventyComputed:
  eleventyNavigation:
    key: "{{ page.url }}"
    title: "{{ title | safe }}"
    parent: "{{ '../' | siteUrl(page.url) }}"
---

{{ content | safe }}