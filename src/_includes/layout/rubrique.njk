---
layout: layout/base.njk

eleventyComputed:
  eleventyNavigation:
    key: "{{ page.url }}"
    title: "{{ title | safe }}"
    parent: "{{ '../' | siteUrl(page.url) }}"
---

<h1 class="text-5xl pt-4 pb-6">{{ title }}</h1>

{{ content | safe }}



